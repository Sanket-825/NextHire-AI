import PagePlaceholder from "../../../components/ui/PagePlaceholder";

export default function ResetPasswordPage() {
  return <PagePlaceholder name="Reset Password" note="New password form, uses :token from URL" />;
}