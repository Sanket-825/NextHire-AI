import PagePlaceholder from "../../../components/ui/PagePlaceholder";

export default function LoginPage() {
  return <PagePlaceholder name="Login" note="Email + password form, link to register" />;
}