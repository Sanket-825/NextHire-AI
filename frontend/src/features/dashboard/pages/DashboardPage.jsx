import PagePlaceholder from "../../../components/ui/PagePlaceholder";

export default function DashboardPage() {
  return <PagePlaceholder name="Dashboard" note="Greeting, stats cards, progress ring, charts, timeline" />;
}