import PagePlaceholder from "../../../components/ui/PagePlaceholder";

export default function InterviewSessionPage() {
  return <PagePlaceholder name="Interview Session" note="Question cards, timer, answer editor, AI feedback panel" />;
}